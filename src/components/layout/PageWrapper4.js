import React from 'react';

import ScrollableListContainer from 'components/lists/ScrollableListContainer';
import FormSelectorContainer from 'components/forms/FormSelectorContainer';

const PageWrapper4 = (props) => {
  return (
    <FormSelectorContainer formsToSync={'formData'}/>
  )
};

export default PageWrapper4;
